function solution(n_str) {
    // n_str = n_str.split('')
    // for(let i=0; i<n_str.length; i++) {
    //     if(n_str[i] !== '0')
    //         return n_str.slice(i).join('')
    // }
    return String(Number(n_str))
}