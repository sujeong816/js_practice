1. 윈도우즈와 비교하여 리눅스 OS의 장점 3가지 이상, 단점 2가지 이상 적어라.
1) 리눅스의 장점
-
-
-
-
2) 리눅스의 단점
-
-
-

2. 다음은 명령 창에서 많이 사용되는 특수 문자들이다. 각 명령의 의미는 무엇인가?
1) 명령 > 파일 :
2) 명령 < 파일 :
3) 명령 >> 파일 :
4) 명령1 | 명령2 :
5) 명령2 > 파일 :

3. 다음은 배쉬 쉘에 관한 명령이다.
1) 사용자 로그인 쉘을 본 쉘로 바꾸는 명령을 입력하라. 본셀은 /bin/sh에 있고, 사용자 명은 test이다.

2) 배시 셀의 출력 명령어를 사용하여 'I love You'를 화면에 출력하는 명령어를 적어라

3) 쉘에서 'ls *.*'를 하였을 때, '*'의 의미는 무엇을 의미하는가?

4) 셀에서 'ls a?.*'를 입력하였을 때 '?'의 의미는 무엇인가?


5. 다음은 vi 에디터에서 많이 사용되는 키들이다.
1) esc+k:
2) esc+r:
3) esc+i:
4) esc+x:
5) esc+yy:
6) sec+P:
7) :! ls:
8) :r test:
9) :wq!:
10) set nu:

6. PC에서 우분투를 설치 또는 사용하기 위한 방법을 최소한 3가지 이상 적고 간단히 설명하라
-
-
-

7. 다음은 디렉토리 표현 방법이다.
1) .. :
2) .:
3) ~:
4) /:

8. 다음은 리눅스에서 '/'밑의 디렉토리의 주요 기능에 대하여 설명하라.
1) /home:
2) /bin:
3) /mnt:
4) /etc:
5) /tmp:
6) /var:
7) /dev:
8) /sys:
9) /root:
10) /lost+found:

9. 다음은 리눅스의 파일 시스템 구조이다.
            /
    home    etc     usr
a   b       c       d   e

1) 현재 작업 디렉토리가 어디 있는지 모른다. 절대 경로 지정 방식으로 작업 디렉토리를 c로 바꾸는 명령

2) 현재 작업 디렉토리가 a에 있다. 상대 경로 지정 방식으로 작업 디렉토리를 d로 바꾸는 명령

3) 현재 작업 디렉토리가 a에 있다. 혀냊 디렉토리 밑에 love라는 서브 디렉토리를 생성하는 명령

4) 현재 작업 디렉토리가 a에 있따. 3)에서 생성된 디렉토리 밑에 you라는 빈 파일을 생성하는 명령 한 줄로 작성

5) 상기 home 디렉도리 밑의 a 파일의 내용과 b 파일의 내용을 합쳐서 k 파일에 저장하고 싶다.


10. 상기 디렉토리 구조에서 /home 밑에 /etc/c 원본 파일의 링크를 설정하려고 한다. 현재 작업 디렉토리는 /home
1) 현재 디렉토리에서 f라는 파일 이름으로 하드 링크를 설정하는 명령

2) /etc/c 원본 파일에 대해 현재 디렉토리에서 g라는 파일 이름으로 심벌릭 링크 설정

3) 만약 rm /etc/c 명령을 사용하여 원본 파일을 지웠다. /home 밑에서 내용을 볼 수 있는 파일은 f와 g 중 어떤 파일인가? (사실 /etc/c는 안 지워짐)


11. test 계정에서 로그인 하자마자 'umask 022'를 입력하였다. 내 홈디렉토리 밑에 서브 디렉토리 a와 서브 파일 b를 생성하였다. 권한 표 작성, 기본 접근 권한 구하기.





12. /home/a 파일의 소유자는 읽기, 쓰기, 실행 가능. 그룹은 읽기, 실행 가능, 기타 사용자는 아무것도 할 수 없음. SetUID와 스티키 비트를 세트하도록 허가권을 부여하는 명령 적기

13. 명령어 결과
1) find /bin +name ls:
2) sudo apt-get install openssh-server:
3) grep host a:
4) file aa:
5) env:
6) ifconfig:
7) man ls:
8) tail -5 aa:
9) history:
10) which ls:
11) pwd:
12) unalias aa:

14. 다음은 쉘 변수와 환경변수이다.
1) 'SOME'이라는 셀 변수에 'test'라는 값을 설정.

2) 상기 설정된 쉘 변수를 환경 변수로 설정하는 명령

3) 설정된 환경 변수를 보는 명령

4) 설정된 환경 변수를 해제하는 명령